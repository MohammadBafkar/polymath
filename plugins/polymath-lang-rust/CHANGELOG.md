# Changelog — polymath-lang-rust

## [Unreleased]

### Added

- Initial v0.1 components: `write-rust-test` skill (#[test]/#[tokio::test]/proptest! patterns, Result-returning tests, assert_matches preferences); `audit-cargo-toml` skill (edition + MSRV + workspace inheritance + features + cargo-audit/cargo-deny); two slash commands wired to the skills.

# Changelog — polymath-connector-grafana

## [Unreleased]

### Added

- Initial v0.1 components: `.mcp.json` referencing a Grafana MCP server; `dashboard-snapshot` skill (absolute timestamps, required template variables enforced, finite TTL, deleteKey saved as artifact); `references/grafana-tools.md`.
- `userConfig.grafanaUrl` and `userConfig.grafanaApiKey` (sensitive — service account token).

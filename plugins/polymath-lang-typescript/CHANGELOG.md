# Changelog — polymath-lang-typescript

## [Unreleased]

### Added

- Initial v0.1 components: `write-vitest-test`, `lint-with-biome`, `migrate-ts-version` skills.
- Commands: `/vitest`, `/biome`, `/ts-migrate` (thin aliases).

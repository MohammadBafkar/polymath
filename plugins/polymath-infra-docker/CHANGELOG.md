# Changelog — polymath-infra-docker

## [Unreleased]

### Added

- Initial v0.1 components: `audit-dockerfile` skill (base-image pinning, multi-stage, USER, HEALTHCHECK, secrets, layer ordering); `audit-compose` skill (image pinning, volumes, networks, secrets, depends_on health-conditioning, port binding); two slash commands.

# Changelog — polymath-connector-statuspage

## [Unreleased]

### Added

- Initial v0.1 components: `.mcp.json` referencing a Statuspage MCP server; `post-statuspage-update` skill (severity → impact mapping + customer-readable body rewrite + component scoping by ID); `references/statuspage-tools.md`.
- `userConfig.statuspageApiKey` (sensitive) and `userConfig.statuspagePageId`.

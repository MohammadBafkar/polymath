# Changelog — polymath-connector-asana

## [Unreleased]

### Added

- Initial v0.1 components: `.mcp.json` referencing an Asana MCP server; `asana-task-update` skill (GID resolution for sections + custom fields + enum options + assignees, section move as a separate call); `references/asana-tools.md`.
- `userConfig.asanaToken` (sensitive) and `userConfig.asanaWorkspaceId`.

# Changelog — polymath-connector-figma

## [Unreleased]

### Added

- Initial v0.1 components: `.mcp.json` referencing a Figma MCP server; `design-spec-handoff` skill (frame URL → component/token/state/copy spec with explicit missing-state flagging + a11y crosscheck); `references/figma-tools.md`.
- `userConfig.figmaAccessToken` (sensitive) and optional `userConfig.figmaTeamId`.

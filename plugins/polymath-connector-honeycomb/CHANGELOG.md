# Changelog — polymath-connector-honeycomb

## [Unreleased]

### Added

- Initial v0.1 components: `.mcp.json` referencing a Honeycomb MCP server; `trace-investigate` skill (span tree reconstruction, ancestor-walk offending-span detection, self-time latency breakdown, evidence-cited hypothesis); `references/honeycomb-tools.md`.
- `userConfig.honeycombApiKey` (sensitive) and `userConfig.honeycombDataset`.

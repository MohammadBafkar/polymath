# Routing declaration for polymath-devops:dockerize (skill). Compiled into
# route-signals.json by tools/build-surface-index.py.
# Schema: registry/schemas/surface-routing.schema.json
paths:
- Dockerfile
intents:
- run the same on every machine
- package it up so
- write a dockerfile
- dockerize the service
- containerize the app

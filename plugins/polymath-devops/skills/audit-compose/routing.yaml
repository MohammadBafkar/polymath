# Routing declaration for polymath-devops:audit-compose (skill). Compiled into
# route-signals.json by tools/build-surface-index.py.
# Schema: registry/schemas/surface-routing.schema.json
paths:
- docker-compose
- compose.yaml
- compose.yml
intents:
- local dev stack
- why half of them exist
- audit the compose file
- review docker-compose
- compose file review

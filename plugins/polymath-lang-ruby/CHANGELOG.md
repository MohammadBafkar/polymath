# Changelog — polymath-lang-ruby

## [Unreleased]

### Added

- Initial v0.1 components: `write-rspec-test` skill (describe/context/it, `let`/`subject`, `instance_double`, `shared_examples`); `audit-gemfile` skill (Ruby version, pinning, bundler-audit, sources, group separation); two slash commands.

# Changelog — polymath-content

## [Unreleased]

### Added

- Initial v0.1 components: `write-release-notes` (customer-facing, headline-first, breaking changes called out), `write-sunset-notice` (two-dates contract + before/after migration), `write-advisory` (security or breaking-change with verification + credit) skills.
- Materialized `Sunset-notice.md` + `Advisory.md` templates from `shared/templates/`.

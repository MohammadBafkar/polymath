# Changelog — polymath-connector-launchdarkly

## [Unreleased]

### Added

- Initial v0.1 components: `.mcp.json` referencing a LaunchDarkly MCP server; `flag-rollout-plan` skill (rollout staging from internal → beta → canary → ramp with success/abort criteria + required retirement date); `references/launchdarkly-tools.md`.
- `userConfig.launchdarklyApiKey` (sensitive) and `userConfig.launchdarklyProject`.

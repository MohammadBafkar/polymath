# Changelog — polymath-author

## [Unreleased]

### Added

- Initial v0.1 components: `validate-plugin`, `token-budget-report`, `skill-author-critic` skills.
- Commands: `/new-plugin`, `/new-skill`, `/new-connector` (thin aliases over `tools/new-*.sh`).
- References: `skill-style-guide.md`, `frontmatter-cheatsheet.md`.

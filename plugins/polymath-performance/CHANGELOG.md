# Changelog — polymath-performance

## [0.1.0]

### Added

- Initial v0.1 components: `perf-budget` (one CUJ, one percentile, component breakdown, tripwire at 80%), `backend-tail-latency` (7-cause walkthrough; ONE dominant cause + first fix), `caching-tradeoffs` (the 5 hard questions + named failure modes) skills.

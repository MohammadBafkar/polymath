# Changelog — polymath-infra-redis

## [Unreleased]

### Added

- Initial v0.1 components: `design-cache-layer` skill (versioned key schema, TTL+jitter, NX-lock stampede protection, hot/big-key avoidance, observability checklist); `audit-redis-config` skill (memory, persistence, replication, slow log, ACL, TLS); two slash commands.

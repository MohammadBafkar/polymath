# Changelog — polymath-infra-postgres

## [0.1.0]

### Added

- Initial v0.1 components: `review-migration` skill (per-statement lock + cost classification, multi-step NOT NULL / FK / index rollout patterns, lock_timeout discipline); `audit-pg-config` skill (memory, connections, WAL, autovacuum, logging); two slash commands.

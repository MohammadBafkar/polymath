# Changelog — polymath-lang-kotlin

## [Unreleased]

### Added

- Initial v0.1 components: `write-kotlin-test` skill (JUnit 5 + MockK + `runTest`; Kotest support when present); `audit-gradle-kts` skill (Kotlin/JVM alignment, plugin pinning, BOM use, multi-module hygiene, reachable vulnerabilities); two slash commands.

# Changelog — polymath-connector-notion

## [Unreleased]

### Added

- Initial v0.1 components: `.mcp.json` referencing a Notion MCP server; `sync-doc-to-notion` skill (Markdown→blocks, sync-key dedupe, repo-source backlink, drift detection); `references/notion-tools.md`.
- `userConfig.notionToken` (sensitive) and `userConfig.notionRootPageId`.

# Changelog — polymath-lang-go

## [Unreleased]

### Added

- Initial v0.1 components: `write-go-test` skill (table-driven + subtests + `t.Cleanup` + stdlib fakes); `audit-go-mod` skill (toolchain, indirect bloat, replace directives, govulncheck-reachable); two slash commands wired to the skills.

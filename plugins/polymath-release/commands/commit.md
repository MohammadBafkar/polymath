---
name: commit
description: Stage and craft a Conventional Commits message for the current diff; never commits without showing the user the staged changes and the message first.
---

# /commit

Thin alias for the `polymath-release:commit` skill — full procedure
(Conventional Commits + safety rails: no `--amend`, no `--no-verify`,
no secrets in diff, no commit without user approval) lives in the
skill.

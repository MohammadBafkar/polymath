# Changelog — polymath-connector-vercel

## [Unreleased]

### Added

- Initial v0.1 components: `.mcp.json` referencing a Vercel MCP server; `inspect-deployment` skill (state + build logs + runtime metrics + edge exit codes + previous-READY-production rollback target); `references/vercel-tools.md`.
- `userConfig.vercelToken` (sensitive) and optional `userConfig.vercelTeamId`.

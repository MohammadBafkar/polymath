# Changelog — polymath-engineering

## [0.1.0]

### Added

- Initial v0.1 components: `feature-dev`, `code-review`, `verify-change`, `read-code` skills.
- Hooks: secret-scan (`PreToolUse`), format-if-config (`PostToolUse`).

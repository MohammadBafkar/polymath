# Changelog — polymath-connector-datadog

## [0.1.0]

### Added

- Initial v0.1 components: `.mcp.json` referencing a Datadog MCP server; `author-monitor` + `query-during-incident` skills; `references/datadog-tools.md`.
- `userConfig` for `datadogApiKey`, `datadogAppKey`, `datadogSite` (API + App key both sensitive).

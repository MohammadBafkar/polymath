# Routing declaration for polymath-flows:run-workflow rootCauseAnalysis (workflow). Compiled into
# route-signals.json by tools/build-surface-index.py.
# Schema: registry/schemas/surface-routing.schema.json
paths:
- docs/rca/
intents:
- what is actually causing
- same symptom
- not just patch it
- root cause analysis
- write an rca
- drive to root cause
alt: polymath-thinking:5-whys

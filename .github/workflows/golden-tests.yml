name: golden-tests

on:
  pull_request:
  push:
    branches: [main]

jobs:
  executable-unit:
    name: bin/polymath-flow unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pyyaml jsonschema
      - name: Run python unit tests
        run: python3 -m unittest discover -s plugins/polymath-flows/tests -v

  executable-e2e:
    name: shipFeature scratch-repo e2e
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pyyaml jsonschema
      - name: Walk shipFeature end-to-end against a scratch repo
        run: |
          set -euxo pipefail
          export PFLOW="$PWD/plugins/polymath-flows/bin/polymath-flow"
          scratch="$(mktemp -d)"
          export CLAUDE_PLUGIN_DATA="$scratch/.pdata"
          cd "$scratch"
          git init -q
          git config user.email ci@polymath.test
          git config user.name "CI"
          touch README.md
          git add . && git commit -q -m "init"

          run_json="$($PFLOW start shipFeature --input title='Rate-limit /login' --input scope=small)"
          run_id="$(python3 -c 'import json,sys;print(json.load(sys.stdin)["run_id"])' <<<"$run_json")"

          first="$($PFLOW next "$run_id")"
          test "$(python3 -c 'import json,sys;print(json.load(sys.stdin)["step_id"])' <<<"$first")" = "prd"

          mkdir -p docs/prds docs/pr
          echo "# PRD" > docs/prds/rate-limit-login.md
          echo "## [Unreleased]" > CHANGELOG.md
          echo "# PR" > docs/pr/rate-limit-login.md

          for step in prd acceptance implement review changelog pr; do
            echo "step done" > "${step}.summary.md"
            $PFLOW complete "$run_id" "$step" --summary "${step}.summary.md"
          done

          echo "All tests verified, ran lint and typecheck" > verify.summary.md
          $PFLOW complete "$run_id" verify --summary verify.summary.md

          result="$($PFLOW assert "$run_id")"
          echo "$result"
          test "$(python3 -c 'import json,sys;print(json.load(sys.stdin)["status"])' <<<"$result")" = "completed"
          test "$(python3 -c 'import json,sys;print(json.load(sys.stdin)["checks"])' <<<"$result")" = "4"

  fixtures-parse:
    name: tests/golden frontmatter parses
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pyyaml
      - name: Parse every tests/golden fixture's frontmatter
        run: |
          python3 - <<'PY'
          import pathlib, sys, yaml
          fail = 0
          for md in pathlib.Path("tests/golden").rglob("*.md"):
              if md.name == "README.md":
                  continue
              text = md.read_text()
              if not text.startswith("---"):
                  print(f"::error file={md}::missing frontmatter")
                  fail = 1
                  continue
              _, fm, _ = text.split("---", 2)
              try:
                  data = yaml.safe_load(fm)
              except yaml.YAMLError as e:
                  print(f"::error file={md}::invalid YAML: {e}")
                  fail = 1
                  continue
              keys = {"plugin", "workflow"}
              if not (data.keys() & keys):
                  print(f"::error file={md}::frontmatter must declare plugin or workflow")
                  fail = 1
              if "scenario" not in data:
                  print(f"::error file={md}::frontmatter missing scenario")
                  fail = 1
              if "expect" not in data:
                  print(f"::error file={md}::frontmatter missing expect")
                  fail = 1
          sys.exit(fail)
          PY

  claude-cli-fixtures:
    name: claude CLI fixture run
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pyyaml
      - name: Detect Claude Code auth
        id: auth
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          if [[ -n "${CLAUDE_CODE_OAUTH_TOKEN:-}" ]]; then
            echo "method=oauth" >> "$GITHUB_OUTPUT"
          elif [[ -n "${ANTHROPIC_API_KEY:-}" ]]; then
            echo "method=apikey" >> "$GITHUB_OUTPUT"
          else
            echo "method=none" >> "$GITHUB_OUTPUT"
          fi
      - name: Skip when no auth is available
        if: steps.auth.outputs.method == 'none'
        run: |
          echo "::warning::Neither CLAUDE_CODE_OAUTH_TOKEN nor ANTHROPIC_API_KEY"
          echo "::warning::is set in repo secrets. Skipping live fixture run."
          echo "::warning::To enable: set CLAUDE_CODE_OAUTH_TOKEN from a"
          echo "::warning::Claude.ai subscription (preferred) or ANTHROPIC_API_KEY."
      - name: Install Claude Code CLI
        if: steps.auth.outputs.method != 'none'
        run: |
          curl -fsSL https://claude.ai/install.sh | bash || true
          # If the official installer isn't available in this environment,
          # the npm path is the documented alternative:
          if ! command -v claude >/dev/null 2>&1; then
            npm install -g @anthropic-ai/claude-code
          fi
          claude --version
      - name: Run fixtures via tests/golden/run-fixtures.sh
        if: steps.auth.outputs.method != 'none'
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: bash tests/golden/run-fixtures.sh

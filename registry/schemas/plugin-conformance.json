{
  "$schema": "./plugin-conformance.schema.json",
  "schemaVersion": 0.1,
  "criteria": [
    {
      "id": "MANIFEST-1",
      "level": "required",
      "description": "plugin.json passes `claude plugin validate --strict` with zero errors.",
      "evidence": "claude plugin validate --strict <plugin_dir>"
    },
    {
      "id": "MANIFEST-2",
      "level": "required",
      "description": "plugin.json has `name`, `version`, `description` (≤ 200 chars), `license`, `author`, and `dependencies` (may be empty for foundation plugins).",
      "evidence": "JSON field presence check"
    },
    {
      "id": "MANIFEST-3",
      "level": "required",
      "description": "Each plugin declares a marketplace maturity `status` in `.claude-plugin/marketplace.json`: stable, beta, experimental, or deprecated.",
      "evidence": "marketplace.json status field check"
    },
    {
      "id": "BUDGET-1",
      "level": "required",
      "description": "Plugin's always-on listing cost ≤ 400 tokens per `claude plugin details`.",
      "evidence": "claude plugin details <plugin>@<marketplace>"
    },
    {
      "id": "DOCS-1",
      "level": "required",
      "description": "Plugin has README.md and CHANGELOG.md at the plugin root.",
      "evidence": "file existence"
    },
    {
      "id": "SKILL-1",
      "level": "required",
      "description": "Every SKILL.md is ≤ 500 lines and has frontmatter with `name` and `description` (≤ 200 chars).",
      "evidence": "tools/lint-skills.sh"
    },
    {
      "id": "TEMPLATE-1",
      "level": "required",
      "description": "If `.claude-plugin/templates.json` exists, every named template materializes from `registry/templates/` without error.",
      "evidence": "tools/link-templates.sh"
    },
    {
      "id": "WORKFLOW-1",
      "level": "required",
      "description": "Every `workflows/*.yaml` validates against `registry/schemas/workflow.schema.json` via `bin/polymath-flow validate`.",
      "evidence": "plugins/polymath-flows/bin/polymath-flow validate"
    },
    {
      "id": "CONNECTOR-1",
      "level": "required",
      "description": "Connector plugins ship `.mcp.json`, `userConfig` with `title` + `description` + `sensitive: true` on credentials, and a `references/<service>-tools.md` doc.",
      "evidence": "structural check on plugin tree"
    },
    {
      "id": "FIXTURE-1",
      "level": "required",
      "description": "Plugin has at least one fixture under `tests/golden/<plugin>/<scenario>.md` with parseable YAML frontmatter declaring `plugin`, `scenario`, and `expect`.",
      "evidence": "tests/golden/<plugin>/*.md"
    },
    {
      "id": "DESCRIPTION-1",
      "level": "recommended",
      "description": "Plugin description and skill descriptions lead with an imperative verb (drives skill discovery).",
      "evidence": "regex check on description fields"
    },
    {
      "id": "HOOK-1",
      "level": "recommended",
      "description": "Hook scripts are quiet by default — only emit when they have something actionable to surface.",
      "evidence": "manual review"
    },
    {
      "id": "SECRET-1",
      "level": "required",
      "description": "No secrets (API keys, tokens, PII) in any committed file. Verified by the `polymath-engineering` secret-scan hook plus a final pre-submission grep.",
      "evidence": "grep + secret-scan.sh"
    }
  ]
}
